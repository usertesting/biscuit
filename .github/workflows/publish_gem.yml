name: Publish Gem
on:
  push:
    branches:
      - "master"
      - "github-workflow-publish-gem-test"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Print GitHub event action
        run: |
          echo "${{ github.event }}"
