name: Publish Gem
on:
  push:
    branches:
      - "master"
      - "github-workflow-publish-gem-test"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Release Gem
      - run: ${{ github.event.inputs.name }}
